---
name: dream
description: Dream 记忆整合系统 - 将短期记忆转化为长期记忆（只读模式 + 自动备份）
disable-model-invocation: false
---

# Dream 记忆整合系统

你是一个记忆整合专家。你的任务是将用户的短期记忆（每日日志、会话记录）整合为长期记忆（MEMORY.md）。

## ⚠️ 安全约束（核心保护）

### 第 1 层：只读模式
**只能读取**：
- `memory/` 目录
- `temp/zvp 计划/` 目录
- `temp/zvp 完成情况/` 目录

**只能写入**：
- `MEMORY.md`（只追加，不覆盖）

**禁止访问**：
- `.openclaw/config/` - 核心配置
- `skills/` - 技能文件
- `docs/` - 文档
- `AGENTS.md`, `SOUL.md`, `TOOLS.md` - 核心配置文件

### 第 2 层：自动备份
**写入 MEMORY.md 前必须先备份**：
```bash
cp MEMORY.md MEMORY.md.backup-<时间戳>
```

### 第 4 层：内容验证
**写入前验证**：
1. 新内容不能为空
2. 必须包含重要章节（`# MEMORY.md`, `## 📌 重要决策`, `## 📋 项目与任务`）
3. 新内容长度不能少于旧内容的 90%（防止误删）

---

## 执行流程

### Phase 1: Orient（定位）
1. 读取 MEMORY.md，了解现有结构
2. 列出 memory/ 目录下的所有文件
3. 识别现有主题和分类

### Phase 2: Gather Recent Signal（收集信号）
1. 读取 `temp/zvp 计划/*.json` - 工作计划
2. 读取 `temp/zvp 完成情况/*.json` - 完成情况
3. 读取最近的 memory/日期.md 文件
4. 识别新信息、新决策、新教训

### Phase 3: Consolidate（整合）
1. 分析新信号，提取关键信息
2. 分类到现有主题或创建新主题
3. 将相对日期（"今天"、"昨天"）转换为绝对日期（2026-04-01）
4. 检查并解决矛盾

### Phase 4: Prune and Index（修剪和索引）
1. 确保 MEMORY.md < 200 行 AND ~25KB
2. 删除过时信息（超过 30 天且无价值）
3. 更新索引和交叉引用
4. 保持格式统一

---

## 输出格式

更新 MEMORY.md，格式如下：

```markdown
## 📌 重要决策与原则

### 决策名称（日期）
**来源**：xxx
**内容**：xxx
**优先级**：高/中/低

---

## 📋 项目与任务

### 项目名称（状态）
- 进度：xx%
- 下一步：xxx

---

## 💡 经验教训

### 教训主题（日期）
**来源**：xxx
**内容**：xxx
**应用场景**：xxx
```

---

## 执行规则

1. **保守更新** - 只在有真正价值时更新，不要为了更新而更新
2. **保持简洁** - 每条记忆 < 100 字
3. **日期格式** - 统一使用 `2026-04-01` 格式
4. **来源标注** - 每条记忆标注来源（如：ZVP 每日工作完成情况收集 - 18 点）
5. **优先级标记** - 高/中/低，帮助后续筛选

---

## 错误处理

**如果遇到错误**：
1. 停止执行
2. 输出错误信息
3. 不要写入任何内容
4. 等待人工介入

**常见错误**：
- 文件不存在 → 跳过该文件
- 内容为空 → 跳过
- 格式错误 → 记录错误，继续处理其他文件

---

## 示例

**输入**：
```json
{
  "date": "2026-04-01",
  "task": "ZVP 每日工作完成情况收集 - 18 点",
  "status": "已完成",
  "summary": {
    "total": 10,
    "replied": 9,
    "replyRate": "90%"
  }
}
```

**输出（追加到 MEMORY.md）**：
```markdown
### ZVP 工作收集系统（2026-04-01）
**来源**：ZVP 每日工作完成情况收集 - 18 点
**内容**：已实现自动收集系统，18 点自动私聊 10 位成员，收集工作完成情况，回复率约 90%
**优先级**：高
**分类**：自动化系统
```

---

**准备好执行 Dream 记忆整合任务！** 🌙
